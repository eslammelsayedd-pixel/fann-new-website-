// The React module augmentation for <model-viewer> has been moved to src/types.ts.
// This is to ensure it is correctly processed by the TypeScript compiler, which is likely
// configured to only include files within the 'src' directory.
export * from './src/types';
